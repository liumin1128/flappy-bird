// bird-store.ts: 小鸟状态管理（可选，当前未用到全局 store，仅类型定义）🐱 喵～
import type { BirdState } from "../flappy-bird/_components/useBird";

export type { BirdState };
// 🐱 喵～
